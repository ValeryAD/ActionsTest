name: Using artifacts
on: 
  workflow_dispatch:
    jobs:
      job1:
      name: Generate artifact
      runs-on: ubuntu-latest
        steps:
          run: |
            "this text for artifact" >> my_artifact.txt
            cat my_arifact.txt

            
